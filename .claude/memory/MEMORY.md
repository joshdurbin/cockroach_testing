- [User profile](user_profile.md) — role, working style, preferences (gRPC>REST, Docker SDK, russ conventions, destructive defaults, no CLI for data ops)
- [CockroachDB + Toxiproxy bootstrap](crdb_toxiproxy_bootstrap.md) — proxy-before-nodes ordering, cockroach init on RPC port (not SQL), /health endpoint, CONFIGURE ZONE outside migrations, 9-node minimum for resilience, chaos status/clear auto-discover proxies
- [CockroachDB schema patterns](crdb_schema_patterns.md) — goose NO TRANSACTION, consolidate columns into CREATE TABLE (no ALTER TABLE on fresh starts), zone configs outside migrations, RLS setup, SET LOCAL for tenant context, write-side counters, crdb_internal restricted, migrations slow with geo latency
- [Docker SDK patterns](go_docker_sdk_patterns.md) — CopyToContainer vs busybox, stdcopy demux, ContainerWait channels, image build from tar, label-based discovery, DestroyCluster volume bug (collect volumes BEFORE removing containers), v27 module layout
- [pgx v5 patterns](pgx_v5_patterns.md) — DBTX interface, stdlib bridge for goose, PrepareConn, pgtype.UUID, multi-host DSN, ping retry, SET LOCAL vs SET, serialization failure detection, NUMERIC→string override
- [Workload container design](workload_container_design.md) — dual-mode binary, ticker rate control, tenantInsert atomic pattern, home-region routing (tenant writes to home partition only), TenantPool indexed by region+tier, two-pool derivation, Dockerfile GOTOOLCHAIN
- [gRPC + buf patterns](grpc_buf_patterns.md) — reflection one-liner, -plaintext/-insecure, local plugins, 5 TenantService methods, GetRegionStatus most useful during chaos, grpcurl/ghz reference
- [Colima + macOS Docker](colima_macos_docker.md) — IPv6/IPv4 port binding, resource requirements (8 CPU/12 GB for 9-node), clock drift, Docker API stalls, named volumes not pruned by docker system prune
- [Cobra + Viper + Zerolog](cobra_viper_zerolog.md) — BindPFlag required, PersistentFlags scope, SetEnvPrefix, OnInitialize, positional args vs flags, Duration flags, zerolog structured fields
- [Prometheus metrics design](prometheus_metrics_design.md) — label cardinality (no UUID labels), two-label pattern (target+qos), promauto, naming convention, histogram buckets, Grafana provisioning, datasource UID, anonymous access, container hostname scraping
