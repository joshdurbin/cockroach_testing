apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: cdbct-prometheus
    access: proxy
    url: http://cdbct-prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 10s
